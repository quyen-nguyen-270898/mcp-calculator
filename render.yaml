services:
  - type: web
    name: mcp-calculator
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: MCP_ENDPOINT
        sync: false
      - key: PORT
        value: 10000
